<% Review.where(place_id: @review.place_id).each do |review| %>
  <%= review.rating%>
  <%= review.comment%>
  <%= review.place_id%>
  <%= link_to 'Edit', edit_review_path(review) %>
  <br><br/>
<% end %>