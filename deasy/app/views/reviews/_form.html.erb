
<%= simple_form_for @review do |f| %>
    <%= f.input :rating %>
    <%= f.input :comment %>
    <%= f.button  :submit%>
    <%= f.hidden_field :place_id, :value => "#{@review.place_id}"%>
<% end %>
